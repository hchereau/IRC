https://codingfarm.tistory.com/539

socket() :
통신을 위한 OS내부 객체 생성, int fd = socket()
은 통신을 위한 OS 내부 객체 생성, 아직은 포트 없음
bind(fd, addr) 하면 포트 8080 은 이 소켓(fd)에 다른 건 저 소켓에.. 이런 식
OS 라우팅 테이블에 등록 = bind
address 안에 이미 IP 랑 포트가 같이 있음
sockaddr_in addr 안에는 이렇기 때문임

#define	__SOCKADDR_COMMON(sa_prefix) \
  sa_family_t sa_prefix##family

struct sockaddr_in
  {
    __SOCKADDR_COMMON (sin_);
    in_port_t sin_port;			/* Port number.  */
    struct in_addr sin_addr;		/* Internet address.  */

    /* Pad to size of `struct sockaddr'.  */
    unsigned char sin_zero[sizeof (struct sockaddr)
			   - __SOCKADDR_COMMON_SIZE
			   - sizeof (in_port_t)
			   - sizeof (struct in_addr)];
  };



bind :
The bind() system call assigns the local protocol address to a socket. 
The first argument should represent the file descriptor obtained from the socket system call. 
The second argument is a struct that contains the protocol, port number, and IP address that the socket will bind to.

listen : 
to make a waiting list 
when a new request from a client arrives before treating client's request
int listen(int sockfd, int backlog);
backlog - size of waiting list 수신대기열 0 success -1 실패

connect : 
client requestion connection by connect()
accept returns socket connected but connect returns only success or not
server: 듣기 소켓 연결 소켓 분리
client : 구분 없음 socket 함수로 생성된 소켓 하나로 연결 및 통신 모두 관리
// int connect(int sockfd, const struct sockaddr *serv_addr, socklen_t addrlen);


/* Await a connection on socket FD.
   When a connection arrives, open a new socket to communicate with it,
   set *ADDR (which is *ADDR_LEN bytes long) to the address of the connecting
   peer and *ADDR_LEN to the address's actual length, and return the
   new socket's descriptor, or -1 for errors.

   This function is a cancellation point and therefore not marked with
   __THROW.  */
extern int accept (int __fd, __SOCKADDR_ARG __addr,
		   socklen_t *__restrict __addr_len);

/* Send N bytes of BUF to socket FD.  Returns the number sent or -1.

   This function is a cancellation point and therefore not marked with
   __THROW.  */
extern ssize_t send (int __fd, const void *__buf, size_t __n, int __flags);

...


poll : 이 fd 들 중에 지금 읽을 수 있는 애 쓸 수 있는 애 누구야? (직접 요청은 recv 해야 알 수 있음) ready or not
poll(fds, nfds, timeout);
결과 :fds[i].revents 에 상태가 찍힘
POLLIN -> 읽을 수 있음 (데이터 도착)
POLLOUT -> 쓸 수 있음
if (fds[i] & POLLIN){
	손 든 애다
}
char buf[1024]; // 값에 대해 생각해보기 효율적인 값이란 무엇일까?
int n = read(fds[i].fd, buf, sizeof(buf));
POLLIN 이 떠도 
데이터 일부만 올 수도 있고
한 번 read(뭐 여기선 recv 겠지..) 로 다 못 읽고 여러번 해야할 수도
실무에선 
버퍼링, 프로토콜 파싱이 필요함 

accept :
!!!!! accept 의 반환값 fd 는 연결 소켓(을 열어준다!)으로 듣기 소켓이랑 다르다!
클라이언트 영역 (클라 인터넷 ) -> 서버 영역(듣기 소켓(socket()) 연결 요청 대기열 (listen()), --accept()-> 연결 소켓)


recv : 
recv (int __fd, void *__buf, size_t __n, int __flags)
{
  size_t sz = __glibc_objsize0 (__buf);
  if (__glibc_safe_or_unknown_len (__n, sizeof (char), sz))
    return __recv_alias (__fd, __buf, __n, __flags);
  if (__glibc_unsafe_len (__n, sizeof (char), sz))
    return __recv_chk_warn (__fd, __buf, __n, sz, __flags);
  return __recv_chk (__fd, __buf, __n, sz, __flags);
}



send :
/* Send N bytes of BUF to socket FD.  Returns the number sent or -1.

   This function is a cancellation point and therefore not marked with
   __THROW.  */
extern ssize_t send (int __fd, const void *__buf, size_t __n, int __flags);

getsockname : 이 소켓이 어떤 주소에 바인딩 됐나 확인
getsockname(fd, (struct sockaddr*)&addr, &len);

!!!!!!! 
server fd 가 손들면
새 손님 왔다는 뜻 : accept
client fd 가 손들면
기존 손님이 데이터 보냈다는 뜻 : recv

poll(fds, nfds, timeout) :
-> server fd 에 POLLIN 발생
-> accept(serv fd, )
-> 새 client fd 얻음
-> 이 c fd 를 poll 목록에 추가

추가? : poll 은 내가 준 fds 배열 안에 있는 애들만 보기 떄문임

fds = [serv fd, client fd] :
배열안에 구조체가 들어있는 배열임
fds[0].fd = serv fd
fds[0].events = POLLIN;
fds[1].fd = client_fd;
fds[1].events = POLLIN


htons :
extern uint16_t htons (uint16_t __hostshort)
     __THROW __attribute__ ((__const__));

pollfd struct : 
/* Data structure describing a polling request.  */
struct pollfd
  {
    int fd;			/* File descriptor to poll.  */
    short int events;		/* Types of events poller cares about.  */
    short int revents;		/* Types of events that actually occurred.  */
  };
